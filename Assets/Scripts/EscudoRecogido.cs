using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EscudoRecogido : MonoBehaviour
//script que detecta si nuestro jugador tiene el escudo en su poder o no
{
    public static bool RecogerEscudo = false; // método que nos indica que en principio no tenemos el escudo y tendremos que encontrarlo
}
