using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RecogerLlave : MonoBehaviour //script que detecta si nuestro jugador tiene la llave en su poder o no
{
    public static bool LlaveRecogida = false; // método que nos indica que en principio no tenemos la llave y tendremos que encontrarla para poder salir
}
